# TacticalSim

TacticalSim is my first and the biggest project written in C++.

---

## Building on macOS

### Prerequisites

- **CMake**: This project uses CMake as its build system.  
  You can install CMake easily via [Homebrew](https://brew.sh/):

```bash
brew install cmake

 after installing Cmake, just double-tap on the buildOnMacOS.sh file ;)
